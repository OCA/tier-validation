Adds an approval workflow to Products using the Tier Validation mechanism.

The default rule requires products to be approved before they can be activated.

The validation is based on the `product_state` module, which provides a state
field on product templates.