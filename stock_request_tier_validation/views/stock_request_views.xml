<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 Jarsa
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="stock_request_search" model="ir.ui.view">
        <field name="name">stock.request.search - stock_request_tier_validation</field>
        <field name="model">stock.request</field>
        <field name="inherit_id" ref="stock_request.stock_request_search" />
        <field name="arch" type="xml">
            <search position="inside">
                <group>
                    <filter
                        name="needs_review"
                        string="Needs my Review"
                        domain="[('reviewer_ids', 'in', uid), ('state', 'not in', ['done', 'cancel'])]"
                        help="Stock Requests to review"
                    />
                    <filter
                        name="tier_validated"
                        string="Validated"
                        domain="[('validation_status', '=', 'validated')]"
                        help="Stock Requests validated and ready to be confirmed"
                    />
                </group>
            </search>
        </field>
    </record>
</odoo>
